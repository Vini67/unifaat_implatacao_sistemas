# TF14 - Arquitetura Resiliente com ALB e Auto Scaling
**Disciplina:** Implementação de Software  
**Curso:** Análise e Desenvolvimento de Sistemas - UniFAAT  
**Aula:** 14 - Revisão Geral e Workshop de Migração  
**Valor:** 5 pontos  
**Prazo:** 1 semana após a aula

---

## 📋 Objetivo

Demonstrar domínio de arquiteturas resilientes usando Application Load Balancer, Auto Scaling Groups e implementação de alta disponibilidade multi-AZ.

---

## 🎯 Tarefa

Criar uma plataforma de streaming de vídeos com arquitetura completamente resiliente, incluindo CDN, auto scaling inteligente e disaster recovery.

---

## 📝 Especificações

### Plataforma de Streaming Resiliente
- **Frontend:** Interface de streaming responsiva
- **API Gateway:** Gerenciamento de APIs
- **Auto Scaling:** Elasticidade baseada em demanda
- **CDN:** CloudFront para distribuição global
- **Disaster Recovery:** Multi-região com failover

### Requisitos Técnicos

**1. Application Load Balancer:**
- Target Groups com health checks avançados
- Sticky sessions para usuários logados
- SSL termination com certificados
- WAF para proteção contra ataques

**2. Auto Scaling Inteligente:**
- Scaling policies baseadas em múltiplas métricas
- Scheduled scaling para picos previsíveis
- Warm-up period configurado
- Cooldown periods otimizados

**3. Alta Disponibilidade:**
- Deployment em 3+ Availability Zones
- RDS Multi-AZ com read replicas
- ElastiCache para session storage
- S3 Cross-Region Replication

---

## 📁 Estrutura de Entrega

```
TF14/
├── README.md
├── infrastructure/
│   ├── alb-setup.sh
│   ├── asg-setup.sh
│   ├── launch-template.json
│   └── cloudformation/
├── application/
│   ├── frontend/
│   ├── backend/
│   └── streaming-api/
├── monitoring/
│   ├── custom-metrics.py
│   ├── scaling-policies.json
│   └── dashboards/
├── disaster-recovery/
│   ├── backup-strategy.md
│   ├── failover-plan.md
│   └── recovery-scripts/
└── load-testing/
    ├── load-test.py
    └── stress-scenarios/
```

---

## 📋 Critérios de Avaliação

### Infraestrutura Resiliente (3 pontos)
- [ ] ALB configurado com SSL (0,8 pt)
- [ ] ASG com scaling policies (1 pt)
- [ ] Multi-AZ deployment (0,6 pt)
- [ ] Health checks avançados (0,6 pt)

### Performance e Escalabilidade (1,5 pontos)
- [ ] CDN configurado (0,5 pt)
- [ ] Cache strategies implementadas (0,4 pt)
- [ ] Load testing realizado (0,3 pt)
- [ ] Otimizações de performance (0,3 pt)

### Disaster Recovery (0,5 pontos)
- [ ] Plano de DR documentado (0,3 pt)
- [ ] Scripts de backup/restore (0,2 pt)

---

## 🏗️ Launch Template Avançado

```json
{
  "LaunchTemplateName": "StreamingPlatform-Template",
  "LaunchTemplateData": {
    "ImageId": "ami-0c02fb55956c7d316",
    "InstanceType": "t3.medium",
    "KeyName": "streaming-platform-key",
    "SecurityGroupIds": ["sg-streaming-app"],
    "IamInstanceProfile": {
      "Name": "StreamingPlatform-InstanceProfile"
    },
    "UserData": "IyEvYmluL2Jhc2gKIyBVc2VyIERhdGEgZm9yIFN0cmVhbWluZyBQbGF0Zm9ybQ==",
    "TagSpecifications": [
      {
        "ResourceType": "instance",
        "Tags": [
          {"Key": "Name", "Value": "StreamingPlatform-Instance"},
          {"Key": "Environment", "Value": "Production"},
          {"Key": "Application", "Value": "StreamingPlatform"},
          {"Key": "AutoScaling", "Value": "true"}
        ]
      }
    ],
    "MetadataOptions": {
      "HttpTokens": "required",
      "HttpPutResponseHopLimit": 2,
      "HttpEndpoint": "enabled"
    },
    "Monitoring": {
      "Enabled": true
    }
  }
}
```

---

## ⚖️ Auto Scaling Policies

```json
{
  "PolicyName": "StreamingPlatform-ScaleUp",
  "PolicyType": "TargetTrackingScaling",
  "TargetTrackingConfiguration": {
    "TargetValue": 70.0,
    "PredefinedMetricSpecification": {
      "PredefinedMetricType": "ASGAverageCPUUtilization"
    },
    "ScaleOutCooldown": 300,
    "ScaleInCooldown": 300
  }
}
```

```bash
# Scaling policy baseado em métricas customizadas
aws application-autoscaling put-scaling-policy \
    --policy-name "StreamingPlatform-RequestsPerTarget" \
    --service-namespace ecs \
    --resource-id service/streaming-cluster/streaming-service \
    --scalable-dimension ecs:service:DesiredCount \
    --policy-type TargetTrackingScaling \
    --target-tracking-scaling-policy-configuration '{
        "TargetValue": 1000.0,
        "PredefinedMetricSpecification": {
            "PredefinedMetricType": "ALBRequestCountPerTarget",
            "ResourceLabel": "app/streaming-alb/1234567890123456/targetgroup/streaming-tg/1234567890123456"
        },
        "ScaleOutCooldown": 300,
        "ScaleInCooldown": 300
    }'
```

---

## 🧪 Load Testing

```python
# load-testing/load-test.py
import asyncio
import aiohttp
import time
import json
from datetime import datetime

class LoadTester:
    def __init__(self, base_url, concurrent_users=100):
        self.base_url = base_url
        self.concurrent_users = concurrent_users
        self.results = []
    
    async def make_request(self, session, endpoint):
        start_time = time.time()
        try:
            async with session.get(f"{self.base_url}{endpoint}") as response:
                response_time = (time.time() - start_time) * 1000
                return {
                    'endpoint': endpoint,
                    'status_code': response.status,
                    'response_time': response_time,
                    'success': response.status == 200
                }
        except Exception as e:
            return {
                'endpoint': endpoint,
                'status_code': 0,
                'response_time': (time.time() - start_time) * 1000,
                'success': False,
                'error': str(e)
            }
    
    async def run_load_test(self, duration_minutes=10):
        """Executar teste de carga por X minutos"""
        end_time = time.time() + (duration_minutes * 60)
        
        endpoints = [
            '/',
            '/api/videos',
            '/api/health',
            '/streaming/popular'
        ]
        
        async with aiohttp.ClientSession() as session:
            while time.time() < end_time:
                tasks = []
                
                # Criar requisições concorrentes
                for _ in range(self.concurrent_users):
                    endpoint = endpoints[len(tasks) % len(endpoints)]
                    task = asyncio.create_task(
                        self.make_request(session, endpoint)
                    )
                    tasks.append(task)
                
                # Aguardar todas as requisições
                results = await asyncio.gather(*tasks)
                self.results.extend(results)
                
                # Aguardar antes da próxima rodada
                await asyncio.sleep(1)
        
        return self.generate_report()
    
    def generate_report(self):
        """Gerar relatório de performance"""
        total_requests = len(self.results)
        successful_requests = sum(1 for r in self.results if r['success'])
        failed_requests = total_requests - successful_requests
        
        response_times = [r['response_time'] for r in self.results if r['success']]
        avg_response_time = sum(response_times) / len(response_times) if response_times else 0
        
        report = {
            'timestamp': datetime.now().isoformat(),
            'total_requests': total_requests,
            'successful_requests': successful_requests,
            'failed_requests': failed_requests,
            'success_rate': (successful_requests / total_requests) * 100,
            'average_response_time': avg_response_time,
            'max_response_time': max(response_times) if response_times else 0,
            'min_response_time': min(response_times) if response_times else 0
        }
        
        return report

# Executar teste
async def main():
    tester = LoadTester('http://streaming-alb-123456789.us-east-1.elb.amazonaws.com')
    report = await tester.run_load_test(duration_minutes=5)
    
    print(json.dumps(report, indent=2))
    
    # Salvar relatório
    with open(f'load-test-report-{int(time.time())}.json', 'w') as f:
        json.dump(report, f, indent=2)

if __name__ == '__main__':
    asyncio.run(main())
```

---

## 📖 README.md Obrigatório

```markdown
# TF14 - Plataforma de Streaming Resiliente

## Aluno
- **Nome:** [Seu Nome Completo]
- **RA:** [Seu RA]

## Arquitetura Implementada
- **ALB:** Application Load Balancer com SSL
- **ASG:** Auto Scaling Group inteligente
- **Multi-AZ:** 3 Availability Zones
- **CDN:** CloudFront global
- **Cache:** ElastiCache Redis
- **Database:** RDS Multi-AZ com read replicas

## Funcionalidades de Resiliência
- ✅ Auto scaling baseado em CPU e requests/target
- ✅ Health checks em múltiplas camadas
- ✅ Failover automático entre AZs
- ✅ Self-healing de instâncias
- ✅ Zero-downtime deployments
- ✅ Disaster recovery multi-região

## Configurações de Scaling

### Target Tracking Policies
- **CPU Target:** 70%
- **Requests per Target:** 1000
- **Scale Out Cooldown:** 300s
- **Scale In Cooldown:** 300s

### Scheduled Scaling
- **Peak Hours:** 18:00-22:00 (min: 4, max: 10)
- **Off Hours:** 02:00-06:00 (min: 2, max: 4)

## Performance Benchmarks
- **Response Time:** < 200ms (p95)
- **Availability:** 99.9%
- **Throughput:** 10,000 requests/minute
- **Error Rate:** < 0.1%

## Load Testing Results
```bash
# Executar teste de carga
python load-testing/load-test.py

# Resultados esperados:
# - 10,000+ requests processadas
# - Success rate > 99%
# - Response time < 500ms
```

## Disaster Recovery
- **RTO:** 15 minutos
- **RPO:** 5 minutos
- **Backup Strategy:** Snapshots automáticos + Cross-region
- **Failover:** Automático via Route 53 health checks

## Custos Estimados
- **ALB:** ~$16/mês
- **EC2 Instances:** ~$30/mês (2-4 instâncias)
- **RDS Multi-AZ:** ~$28/mês
- **CloudFront:** ~$10/mês
- **Total:** ~$84/mês
```

---

## 🚀 Entrega

### Repositório GitHub
- **Nome:** `tfsImplantacaoSistemas2026`
- **Pasta:** `TF14/`

### Demonstração Obrigatória
- Mostrar auto scaling em ação
- Simular falha de AZ
- Demonstrar self-healing
- Apresentar métricas de performance

---

**Construa sistemas indestrutíveis! 🏗️**